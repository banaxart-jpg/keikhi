# admin : 管理ダッシュボード (Firebase Hosting only, no Cloud Run)
# Static HTML + Firebase Auth + direct GCP API calls from the browser.
name: admin
display_name: 管理ダッシュボード
HAS_DB: "false"
HAS_BUCKET: "false"
HAS_CLOUD_RUN: "false"
CLOUDBUILD: apps/admin/cloudbuild.yaml
